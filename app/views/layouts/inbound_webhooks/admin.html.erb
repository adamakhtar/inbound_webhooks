<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Inbound Webhooks Admin</title>
  <style>
    :root {
      --color-bg: #f8f9fa;
      --color-surface: #ffffff;
      --color-border: #dee2e6;
      --color-text: #212529;
      --color-text-muted: #6c757d;
      --color-primary: #0d6efd;
      --color-primary-hover: #0b5ed7;
      --radius: 6px;
    }

    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    body {
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
      font-size: 14px;
      line-height: 1.5;
      color: var(--color-text);
      background: var(--color-bg);
    }

    .iw-admin { max-width: 1200px; margin: 0 auto; padding: 24px 16px; }

    .iw-admin h1 {
      font-size: 22px;
      font-weight: 600;
      margin-bottom: 20px;
    }

    .iw-filters {
      background: var(--color-surface);
      border: 1px solid var(--color-border);
      border-radius: var(--radius);
      padding: 16px;
      margin-bottom: 20px;
    }

    .iw-filters form { display: flex; flex-wrap: wrap; gap: 16px; align-items: flex-end; }

    .iw-filter-group {
      display: flex;
      flex-direction: column;
      gap: 4px;
    }

    .iw-filter-group label {
      font-size: 12px;
      font-weight: 600;
      color: var(--color-text-muted);
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    .iw-filter-group select,
    .iw-filter-group input[type="date"],
    .iw-filter-group input[type="datetime-local"] {
      padding: 6px 10px;
      border: 1px solid var(--color-border);
      border-radius: var(--radius);
      font-size: 13px;
      background: var(--color-surface);
      color: var(--color-text);
    }

    .iw-checkbox-group { display: flex; flex-wrap: wrap; gap: 8px; }

    .iw-checkbox-group label {
      font-size: 13px;
      font-weight: 400;
      text-transform: none;
      letter-spacing: 0;
      color: var(--color-text);
      display: flex;
      align-items: center;
      gap: 4px;
      cursor: pointer;
    }

    .iw-presets { display: flex; gap: 4px; flex-wrap: wrap; }

    .iw-presets a,
    .iw-presets span {
      font-size: 12px;
      padding: 2px 8px;
      border-radius: var(--radius);
      text-decoration: none;
      border: 1px solid var(--color-border);
      color: var(--color-text-muted);
      background: var(--color-surface);
    }

    .iw-presets span.active {
      background: var(--color-primary);
      color: #fff;
      border-color: var(--color-primary);
    }

    .iw-btn {
      display: inline-block;
      padding: 6px 16px;
      font-size: 13px;
      font-weight: 500;
      color: #fff;
      background: var(--color-primary);
      border: 1px solid var(--color-primary);
      border-radius: var(--radius);
      cursor: pointer;
      text-decoration: none;
    }

    .iw-btn:hover { background: var(--color-primary-hover); }

    .iw-btn-outline {
      background: var(--color-surface);
      color: var(--color-text-muted);
      border-color: var(--color-border);
    }

    .iw-btn-outline:hover { background: var(--color-bg); color: var(--color-text); }

    .iw-table-wrap {
      background: var(--color-surface);
      border: 1px solid var(--color-border);
      border-radius: var(--radius);
      overflow-x: auto;
    }

    .iw-table { width: 100%; border-collapse: collapse; }

    .iw-table th {
      text-align: left;
      padding: 10px 12px;
      font-size: 12px;
      font-weight: 600;
      color: var(--color-text-muted);
      text-transform: uppercase;
      letter-spacing: 0.5px;
      border-bottom: 2px solid var(--color-border);
      white-space: nowrap;
    }

    .iw-table td {
      padding: 10px 12px;
      border-bottom: 1px solid var(--color-border);
      font-size: 13px;
      vertical-align: top;
    }

    .iw-table tr:last-child td { border-bottom: none; }
    .iw-table tbody tr:hover { background: #f1f3f5; }

    .iw-badge {
      display: inline-block;
      padding: 2px 8px;
      border-radius: 10px;
      font-size: 12px;
      font-weight: 500;
      line-height: 1.4;
    }

    .iw-badge-pending    { background: #fff3cd; color: #856404; }
    .iw-badge-processing { background: #cce5ff; color: #004085; }
    .iw-badge-processed  { background: #d4edda; color: #155724; }
    .iw-badge-retrying   { background: #ffe8cc; color: #854d0e; }
    .iw-badge-failed     { background: #f8d7da; color: #721c24; }
    .iw-badge-unhandled  { background: #e2e3e5; color: #383d41; }

    .iw-error-msg {
      color: var(--color-text-muted);
      font-size: 12px;
      max-width: 300px;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }

    .iw-pagination {
      display: flex;
      justify-content: center;
      padding: 16px;
      gap: 4px;
    }

    .iw-pagination a,
    .iw-pagination em,
    .iw-pagination span {
      display: inline-block;
      padding: 4px 10px;
      border: 1px solid var(--color-border);
      border-radius: var(--radius);
      font-size: 13px;
      font-style: normal;
      text-decoration: none;
      color: var(--color-text);
    }

    .iw-pagination a:hover { background: var(--color-bg); }
    .iw-pagination a.current, .iw-pagination em { background: var(--color-primary); color: #fff; border-color: var(--color-primary); }
    .iw-pagination .disabled { color: var(--color-text-muted); cursor: default; }

    .iw-empty {
      text-align: center;
      padding: 40px 16px;
      color: var(--color-text-muted);
    }

    .iw-date-filter-row {
      display: flex;
      align-items: center;
      gap: 6px;
    }

    .iw-clickable-row { cursor: pointer; }

    /* Show page */
    .iw-show-header {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      gap: 16px;
      margin-bottom: 24px;
    }

    .iw-show-header h1 { font-size: 22px; font-weight: 600; margin: 0; }
    .iw-show-id { font-size: 13px; color: var(--color-text-muted); }

    .iw-detail-grid {
      display: grid;
      grid-template-columns: 1fr;
      gap: 16px;
    }

    .iw-detail-card {
      background: var(--color-surface);
      border: 1px solid var(--color-border);
      border-radius: var(--radius);
      padding: 20px;
    }

    .iw-detail-card h2 {
      font-size: 14px;
      font-weight: 600;
      color: var(--color-text-muted);
      text-transform: uppercase;
      letter-spacing: 0.5px;
      margin-bottom: 12px;
    }

    .iw-detail-card-error { border-left: 3px solid #dc3545; }

    .iw-dl { display: grid; grid-template-columns: 160px 1fr; gap: 8px 16px; }
    .iw-dl dt { font-weight: 600; font-size: 13px; color: var(--color-text-muted); }
    .iw-dl dd { font-size: 13px; margin: 0; }

    .iw-error-pre {
      font-size: 13px;
      white-space: pre-wrap;
      word-break: break-word;
      color: #dc3545;
      margin: 0;
    }

    .iw-json {
      background: #1e1e2e;
      color: #cdd6f4;
      border-radius: var(--radius);
      padding: 16px;
      overflow-x: auto;
      font-size: 13px;
      line-height: 1.6;
      margin: 0;
    }

    .iw-json code { font-family: "SF Mono", "Fira Code", "Fira Mono", Menlo, Consolas, monospace; }
    .iw-json-key { color: #89b4fa; }
    .iw-json-string { color: #a6e3a1; }
    .iw-json-number { color: #fab387; }
    .iw-json-bool { color: #cba6f7; }
  </style>
</head>
<body>
  <div class="iw-admin">
    <%= yield %>
  </div>
</body>
</html>
